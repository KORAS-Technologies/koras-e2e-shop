# Fly configuration for the provisioning and reconciliation worker.
#
# The app name is supplied per environment on the command line, not written
# here. One file serves four estates, and no file in the repository names
# production -- so a hurried `flyctl deploy` cannot pick production up as its
# default.
#
# There is no [processes] block: the Dockerfile's CMD is what runs. Naming the
# command again here means two places to change and one of them will be missed.

primary_region = "iad"

[build]

# No http_service and no autostop. A queue consumer receives no inbound
# requests, so a rule that watches HTTP traffic would stop it and never start
# it again -- the queue would fill silently, which is the failure mode hardest
# to notice and most expensive to find.

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 512
