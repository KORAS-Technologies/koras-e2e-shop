bind 0.0.0.0
protected-mode no
port 6379
loglevel notice

# Persistence — save to disk every 60s if ≥1 key changed
save 60 1
dir /data
dbfilename dump.rdb

# Max memory — evict LRU when full (suitable for dev queue + cache)
maxmemory 256mb
maxmemory-policy allkeys-lru
