{
  "$comment": [
    "The private API every KORAS product exposes, and the Control Plane calls.",
    "",
    "One declaration, read by both sides. The route list used to exist three times:",
    "in the product's router, in the product's contract test, and again in the",
    "Control Plane's reference implementation. Three copies of a contract is three",
    "chances for two of them to agree while the third ships.",
    "",
    "The Control Plane's tests/contract/reference_product.py says it mirrors the",
    "starter's router and that 'if the two drift, these tests are the place that",
    "should hurt' -- but nothing compared them. This is the thing that compares.",
    "",
    "Both profiles ship an identical copy, and the starter's shared-template parity",
    "test fails if they stop matching. Versioned in the path: a breaking change is",
    "a v2 running alongside v1 until every registered product has migrated."
  ],
  "version": "v1",
  "prefix": "/internal/platform/v1",
  "routes": [
    { "method": "post", "path": "/tenants", "summary": "Create a tenant, or return the existing one" },
    { "method": "get", "path": "/tenants/{tenant_id}", "summary": "Read a tenant" },
    { "method": "post", "path": "/tenants/{tenant_id}/activate", "summary": "Activate a tenant" },
    { "method": "post", "path": "/tenants/{tenant_id}/suspend", "summary": "Suspend a tenant" }
  ],
  "rules": {
    "machine_identity_only": {
      "requirement": "Every route rejects a human token, whoever it belongs to.",
      "reason": "None of these are part of an interactive flow, so a browser token arriving here is a mistake or an attack -- even an administrator's."
    },
    "create_is_idempotent": {
      "requirement": "A repeated create with the same tenant_key returns the existing tenant with 200, never 201 and never 409.",
      "reason": "tenant_key is client-supplied and deterministic, which is what makes the call retryable. Answering 409 would turn a retry into a failure."
    },
    "no_direct_writes": {
      "requirement": "The Control Plane never writes to product business tables. Every interaction goes through these routes.",
      "reason": "A second writer is a second schema owner, and neither side would know when the other changed."
    },
    "environment_must_match": {
      "requirement": "A create naming an environment other than the one this service serves is refused with 422.",
      "reason": "A misconfigured caller -- a dev Control Plane holding a prod address, or the reverse. Writing the row anyway would put a customer's prod tenant in a dev database, which is the boundary the whole estate is arranged around. 422 and not a retryable code: the request will not become valid by being sent again.",
      "status": 422
    },
    "slug_conflict_is_not_adoptable": {
      "requirement": "A create whose organization slug is already held by a DIFFERENT organization is refused with 409, and the caller must not adopt the existing tenant.",
      "reason": "Distinct from create_is_idempotent, and the distinction is the whole point. A repeat of the same tenant_key is answered 200 with that tenant. This 409 is two different customers asking for one name, so the tenant in the way belongs to somebody else -- adopting it would hand one customer another customer's tenant. Nothing on either side can resolve it without a person choosing a different name, so the provisioning job holds rather than rolling back: the organization and its identity resources are already correct, and destroying them because a name collided punishes the customer for the collision.",
      "status": 409
    }
  }
}
