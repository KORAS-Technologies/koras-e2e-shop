[project]
name = "koras-logging"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["structlog>=24.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/koras_logging"]
